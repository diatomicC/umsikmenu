import React, { useEffect, useState } from "react";

const CategorySlider = () => {
  return (
    <div style={{height: "64px", width: "100%", backgroundColor: "#0004"}}>
      {/* category.map(() => {
        return (
          <category btn>
        );
        }) */}
    </div>
  );
};

export default CategorySlider;
